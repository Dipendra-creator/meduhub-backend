PORT=5000
NODE_ENV=development

# Firebase Configuration (Choose ONE method below)

# METHOD 1: Full service account JSON (recommended for deployment platforms like Vercel, Railway, etc.)
# Copy the entire content of your serviceAccountKey.json file as a single line
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"meduhub-52922",...}

# METHOD 2: Individual fields (alternative for some deployment platforms)
FIREBASE_PROJECT_ID=meduhub-52922
FIREBASE_CLIENT_EMAIL=your-firebase-adminsdk@meduhub-52922.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----\n"

# METHOD 3: Local development
# Just place serviceAccountKey.json in the project root (already in .gitignore)
